import { Request, Response } from 'express';
import {} from '../models/tripModels.ts';

async function getTrips(req: Request, res: Response) {
  try {
    // use the models to interact with database
    // process the data
    // send the data back

  } catch (error) {
  }
}

export { getTrips };
